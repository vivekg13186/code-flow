"""REMOVED — this example used the old graph model (next=/condition=/loop=).

The engine is now program-based only: one @flow body of plain Python calling
@step methods. See workflows/examples/ResumeDemo.py and LoopPatterns.py for
the replacements.

This file is a placeholder because the tool that wrote it cannot delete files
on your machine. Delete it.
"""
